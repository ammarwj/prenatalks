# Nginx hanya perlu isi api/public untuk try_files — eksekusi PHP dilempar ke
# container app lewat FastCGI, jadi kode aplikasi tidak perlu ada di sini.
FROM nginx:alpine

COPY api/public /var/www/html/public
COPY docker/nginx/prod.conf /etc/nginx/conf.d/default.conf
